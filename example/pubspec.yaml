name: kickin_example
description: Example app for the kickin package.
publish_to: none

environment:
  sdk: ^3.9.5
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  kickin:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
