name: kickin
description: "A Flutter package that makes developments easier by providing commonly used utilities and widgets with just simple initialization.(In Progress)"
version: 0.0.1-dev.5
homepage: https://github.com/okikday
repository: https://github.com/okikday/kickin

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.4.1
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_animate: ^4.5.2
  flutter_riverpod: ^3.3.1
  flutter_secure_storage: ^10.2.0
  hive_flutter: ^1.1.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

